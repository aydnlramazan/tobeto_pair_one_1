import 'package:flutter/material.dart';

class CodeAcademy extends StatelessWidget {
  const CodeAcademy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
