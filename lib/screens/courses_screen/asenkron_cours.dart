import 'package:flutter/material.dart';

class AsenkronCours extends StatelessWidget {
  const AsenkronCours({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
